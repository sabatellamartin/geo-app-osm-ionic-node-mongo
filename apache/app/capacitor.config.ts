import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.geoapp.app',
  appName: 'geoapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
