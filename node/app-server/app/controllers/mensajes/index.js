const { createMensaje } = require('./createMensaje')
const { deleteMensaje } = require('./deleteMensaje')
const { getAllMensajes } = require('./getAllMensajes')
const { getMensaje } = require('./getMensaje')
const { getMensajes } = require('./getMensajes')
const { updateMensaje } = require('./updateMensaje')

module.exports = {
  createMensaje,
  deleteMensaje,
  getAllMensajes,
  getMensaje,
  getMensajes,
  updateMensaje
}
