const { createRegion } = require('./createRegion')
const { deleteRegion } = require('./deleteRegion')
const { getAllRegiones } = require('./getAllRegiones')
const { getRegion } = require('./getRegion')
const { getRegiones } = require('./getRegiones')
const { updateRegion } = require('./updateRegion')

module.exports = {
  createRegion,
  deleteRegion,
  getAllRegiones,
  getRegion,
  getRegiones,
  updateRegion
}
